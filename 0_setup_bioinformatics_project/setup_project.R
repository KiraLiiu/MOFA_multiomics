

#set back to default root folder in a project
setwd(rstudioapi::getActiveProject())

# create a structured bioinformatics project
# open the "0_setup_project_structure.R" and change the name of your project

source("./0_setup_bioinformatics_project/0_setup_project_structure.R")

#set back to default root folder in a project
setwd(rstudioapi::getActiveProject())
